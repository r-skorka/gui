import { NbgDateFormatPipe } from './nbg-date-format.pipe';

describe('NbgDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NbgDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
